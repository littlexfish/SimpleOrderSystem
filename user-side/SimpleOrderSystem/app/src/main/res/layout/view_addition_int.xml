<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/frame"
    android:padding="10dp">

    <EditText
        android:id="@+id/addIntValue"
        style="@style/Theme.SimpleOrderSystem.EditText"
        android:layout_width="90dp"
        android:layout_height="38dp"
        android:layout_gravity="right|center_vertical"
        android:layout_marginEnd="45dp"
        android:ems="10"
        android:inputType="numberSigned" />

    <TextView
        android:id="@+id/addIntName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="left|center_vertical"
        android:text="TextView"
        android:textSize="@dimen/add_label_size" />

    <ImageView
        android:id="@+id/addIntUp"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="right|center_vertical"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/square_right" />

    <ImageView
        android:id="@+id/addIntDown"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="right|center_vertical"
        android:layout_marginEnd="140dp"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/square_left" />
</FrameLayout>